import React from 'react'
import { Box } from '@mui/material'

const WrapperPage = ({ children }) => {
  return <Box sx={{ marginLeft: '2%', marginRight: '2%' }}>{children}</Box>
}

export default WrapperPage
